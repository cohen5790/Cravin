<%- include('../partials/headerComm') %>

    <table id="list">
        <thead></thead>
        <tr>
            <th>Name: <br></th>
            <th>Address: <br></th>
        </tr>
      </thead>
        <tbody>
          <% restaurants.forEach(function(item) { %>
            <tr>
              <td><%= item.name %><br></td>
              <td><%= item.address %><br></td>
              <td><a href="/commercial/show/<%= item._id %>">DETAILS</a><br></td>
            </tr>
            <% }); %>
          </tbody>
        </table><br><br><br><br>
    <a href="/commercial/new">Add New Restaurant</a>

<%- include('../partials/footer') %>
